fastapi>=0.109.0
uvicorn[standard]>=0.27.0
sqlalchemy>=2.0.0
alembic>=1.13.0
asyncpg>=0.29.0
psycopg2-binary>=2.9.9
redis>=5.0.0
celery[redis]>=5.3.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-multipart>=0.0.6
httpx>=0.26.0
aiofiles>=23.2.1
beets>=1.6.0
# Required by the MusicBrainz link resolver (app/api/routes/beets_autotag.py).
# beets 2.x dropped musicbrainzngs as a dependency — it ships its own MB client
# now — so it must be declared explicitly or the resolver fails at runtime.
musicbrainzngs>=0.7.1
python-slugify>=8.0.0
pyyaml>=6.0.0
watchdog>=3.0.0
mutagen>=1.47.0
pillow>=10.0.0

# Beets plugins ecosystem
pyacoustid>=1.3.0       # chroma plugin (acoustic fingerprinting)
pylast>=5.0.0           # lastgenre plugin
python3-discogs-client>=2.7.0  # discogs plugin
flask>=3.0.0            # web plugin (beets built-in API)
requests>=2.31.0        # embyupdate / spotify / deezer http calls
