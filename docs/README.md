# Documentation

Project docs live here. Start with the one that matches your need:

| File | For |
|---|---|
| [`architecture.md`](architecture.md) | How the services fit together, API conventions, beets gotchas, data flow for key operations, cache keys, filesystem layout. |
| [`development.md`](development.md) | Local dev workflow — first-run setup, the no-hot-reload rule, faster inner loop tricks, conventions. |
| [`testing.md`](testing.md) | Running backend + frontend tests, dev-deps install, test-infra quirks that cost us time, common failure patterns. |
| [`existing-libraries.md`](existing-libraries.md) | How to point the app at a beets library you already have on disk. |

For a quick setup path, the top-level [`../README.md`](../README.md) is the right entry point.
